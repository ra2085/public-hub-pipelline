openapi: 3.0.0
info:
  title: Test Service (API Key Legacy)
  version: 1.0.0
  description: Test service for legacy API Key validation
servers:
  - url: https://api.example.com
paths:
  /test:
    get:
      operationId: getTest
      responses:
        "200":
          description: OK
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-KEY
